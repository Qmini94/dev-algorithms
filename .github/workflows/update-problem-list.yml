name: Update Problem List

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'src/main/java/problems/programmers/**'
      - 'src/test/java/problems/programmers/**'
      - 'docs/problems/programmers/**'
  workflow_dispatch:

jobs:
  update-list:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Generate problem list
        run: |
          python3 << 'EOF'
          import os
          from pathlib import Path
          from collections import defaultdict
          
          # 현재 작업 디렉토리 확인 (디버깅)
          print(f"Current working directory: {os.getcwd()}")
          
          # 절대 경로 사용
          src_main = Path('src/main/java/problems/programmers')
          src_test = Path('src/test/java/problems/programmers')
          docs_path = Path('docs/problems/programmers')
          output_file = Path('problems/programmers.md')
          
          print(f"src_main exists: {src_main.exists()}")
          print(f"src_test exists: {src_test.exists()}")
          print(f"docs_path exists: {docs_path.exists()}")
          
          # 레벨별로 파일 수집
          problems = defaultdict(lambda: {'solution': None, 'test': None, 'doc': None})
          
          # src/main/java 파일 수집
          if src_main.exists():
              for level_dir in sorted(src_main.iterdir()):
                  if level_dir.is_dir():
                      level = level_dir.name.upper()
                      for java_file in sorted(level_dir.glob('*.java')):
                          problem_name = java_file.stem
                          key = f"{level}:{problem_name}"
                          # 단순 문자열 경로 (슬래시로 변환)
                          problems[key]['solution'] = str(java_file).replace('\\', '/')
                          problems[key]['level'] = level
                          problems[key]['name'] = problem_name
                          print(f"Found solution: {problems[key]['solution']}")
          
          # src/test/java 파일 수집
          if src_test.exists():
              for level_dir in sorted(src_test.iterdir()):
                  if level_dir.is_dir():
                      level = level_dir.name.upper()
                      for java_file in sorted(level_dir.glob('*Test.java')):
                          problem_name = java_file.stem.replace('Test', '')
                          key = f"{level}:{problem_name}"
                          # 단순 문자열 경로 (슬래시로 변환)
                          problems[key]['test'] = str(java_file).replace('\\', '/')
                          if 'level' not in problems[key]:
                              problems[key]['level'] = level
                              problems[key]['name'] = problem_name
                          print(f"Found test: {problems[key]['test']}")
          
          # docs/problems/programmers 파일 수집
          if docs_path.exists():
              for level_dir in sorted(docs_path.iterdir()):
                  if level_dir.is_dir():
                      level = level_dir.name.upper()
                      for md_file in sorted(level_dir.glob('*.md')):
                          problem_name = md_file.stem
                          key = f"{level}:{problem_name}"
                          # 단순 문자열 경로 (슬래시로 변환)
                          problems[key]['doc'] = str(md_file).replace('\\', '/')
                          if 'level' not in problems[key]:
                              problems[key]['level'] = level
                              problems[key]['name'] = problem_name
                          print(f"Found doc: {problems[key]['doc']}")
          
          # Markdown 생성
          output_file.parent.mkdir(parents=True, exist_ok=True)
          
          with open(output_file, 'w', encoding='utf-8') as f:
              f.write('# Programmers 문제 목록\n\n')
              f.write('> 자동 생성된 문제 목록입니다. 수동으로 편집하지 마세요.\n\n')
              f.write('---\n\n')
          
              if not problems:
                  f.write('문제가 없습니다. 파일을 추가해주세요!\n\n')
              else:
                  # 레벨별로 그룹화
                  levels = defaultdict(list)
                  for key, data in problems.items():
                      level = data.get('level', 'UNKNOWN')
                      levels[level].append(data)
          
                  # 레벨 순서대로 정렬
                  level_order = ['LV0', 'LV1', 'LV2', 'LV3', 'LV4', 'LV5']
          
                  for level in level_order:
                      if level not in levels:
                          continue
          
                      f.write(f'## {level}\n\n')
                      f.write('| 문제명 | 풀이 | 테스트 | 문서 |\n')
                      f.write('|--------|------|--------|------|\n')
          
                      for data in sorted(levels[level], key=lambda x: x['name']):
                          name = data['name']
                          solution_link = f"[📝]({data['solution']})" if data['solution'] else '-'
                          test_link = f"[🧪]({data['test']})" if data['test'] else '-'
                          doc_link = f"[📖]({data['doc']})" if data['doc'] else '-'
          
                          f.write(f'| {name} | {solution_link} | {test_link} | {doc_link} |\n')
          
                      f.write('\n')
          
                  # 통계 추가
                  f.write('---\n\n')
                  f.write('## 📊 통계\n\n')
                  total_problems = len(problems)
                  total_with_solution = sum(1 for p in problems.values() if p['solution'])
                  total_with_test = sum(1 for p in problems.values() if p['test'])
                  total_with_doc = sum(1 for p in problems.values() if p['doc'])
          
                  f.write(f'- 전체 문제: {total_problems}개\n')
                  f.write(f'- 풀이 완료: {total_with_solution}개\n')
                  f.write(f'- 테스트 작성: {total_with_test}개\n')
                  f.write(f'- 문서화 완료: {total_with_doc}개\n')
          
                  if total_problems > 0:
                      completion_rate = (total_with_doc / total_problems) * 100
                      f.write(f'- 문서화율: {completion_rate:.1f}%\n')
          
          print(f"✅ programmers.md 생성 완료!")
          print(f"📝 총 {len(problems)}개 문제 수집")
          EOF

      - name: Check if there are changes
        id: check_changes
        run: |
          git diff --quiet problems/programmers.md || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add problems/programmers.md
          git commit -m "docs: auto-update programmers problem list"
          git push